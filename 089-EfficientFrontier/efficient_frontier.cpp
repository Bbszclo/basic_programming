int main(int argc, char* argv[]) {
    //first step: check valid input 2 input again
    if (argc != 2) {
        //check invalid command line use
        cerr << "Usage: " << argv[0] << " <filename>\n"; 
        return EXIT_FAILURE;
    }
}